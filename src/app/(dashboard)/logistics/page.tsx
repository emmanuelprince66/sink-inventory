// Logistics screen disabled — re-enable by uncommenting the import + render below
// and the sidebar entry in constants/links.ts.

// import Logistics from "./Logistics";

const Page = () => {
  return null;
  // return <Logistics />;
};

export default Page;
